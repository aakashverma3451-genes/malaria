# Web framework
fastapi==0.128.8
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Validation & settings
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Auth (JWT validation from Keycloak)
# NOTE: python-jose pulls in ecdsa, which has an unfixed advisory (PYSEC-2026-1325).
# Not reachable — we only verify RS256, never sign / never use EC keys. See SECURITY.md.
python-jose[cryptography]==3.5.0
httpx==0.28.1

# Rate limiting
slowapi==0.1.9

# Task queue
celery[redis]==5.4.0
redis==5.2.1
flower==2.0.1

# Object storage
minio==7.2.12

# Utilities
python-dotenv==1.2.2
gunicorn==23.0.0

# Transitive pins (known CVEs in older resolved versions)
click==8.4.2
urllib3==2.7.0
